# Gmail OAuth (from Google Cloud Console credentials JSON)
GMAIL_CLIENT_ID=your_client_id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your_client_secret
GMAIL_REDIRECT_URI=http://localhost:3000
GMAIL_REFRESH_TOKEN=
GMAIL_ADDRESS=your@gmail.com

# Optional: for get-new-token.js callback (must match GMAIL_REDIRECT_URI)
# If using http://localhost (no port), add http://localhost:3000 in Google Console and use it here.

# OpenAI
OPENAI_API_KEY=

# SMTP (app uses OAuth; these may be unused)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
